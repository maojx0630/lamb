server:
  port: 8084
spring:
  application:
    name: swa
  cloud:
    nacos:
      config:
        prefix: swa
        server-addr: 127.0.0.1:8848
        file-extension : yml
        shared-dataids: public.yml,discovery.yml,sentinel.yml,redis.yml,datasource.yml,seata.yml
        refreshable-dataids: public.yml,discovery.yml,sentinel.yml,redis.yml,datasource.yml,txlcn.yml